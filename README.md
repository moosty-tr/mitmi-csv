# mitmi-csv
A zero config Modbus to CSV converter.  Just set the Modbus read parameters in URL.  mitmi-csv connects to revice and resturns read values in plain text csv.  Best to use with Excel and Power BI
